<style>
  .search-btn {
    background-color: #fe424d; 
    color:#fff ;
    border-radius: 25px;
    padding: 0 1rem 0 1rem ;
  }
  .search-btn:hover {
    background-color: #ff2e33;
    color:#fff ;
  }
  .search-btn i{
    display: inline;
    margin-right: 0.5rem;
  }
  .search-btn {
    background-color: #fe424d; 
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem;
  }
  .search-btn:hover {
    background-color: #ff2e33;
    color: #fff;
  }
  .search-btn i {
    display: inline;
    margin-right: 0.5rem;
  }
  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 1rem;
  }
  .search-div{
    margin-right: 17rem;
  }
  @media (max-width: 1000px) {
    .search-div {
      display: none;
    }
    .login-btns{
      display: inline-block;
    }
  }
</style>

<nav class="navbar navbar-expand-md bg-body-light sticky-top border-bottom">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/listings">Explore</a>
          </li>
        </ul>
      </div>
      <div class="navbar-nav search-div">
        <form id="searchForm" class="d-flex" role="search">
          <input class="form-control me-2 search-inp" type="search" id="searchInput" placeholder="Search-Country" aria-label="Search">
          <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
      </div>
      <div class="navbar-nav ms-auto login-btns" id="navbarNav">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(!currentUser){ %>
          <a class="nav-link " aria-current="page" href="/signup"><b>Sign-up</b></a>
          <a class="nav-link" href="/login"><b>Login</b></a>
        <% } %>
        <% if(currentUser){ %>
          <a class="nav-link" href="/logout"><b>Logout</b></a>
        <% } %>
      </div>
    </div>
</nav>
<script>
  let input = document.getElementById('searchForm')
  input.addEventListener('submit', function (event) {
    event.preventDefault(); 
    const inputValue = document.getElementById('searchInput').value;

    console.log('Input Value:', inputValue);

    window.location.href = '/filters/new/' + inputValue;  

  });
</script>